<% layout("/layouts/Boilerplate") %>

<!-- Pass Mapbox token and coordinates to external JS -->
<script>
  let maptoken = "<%- MAP_TOKEN %>";  
  const issCoordinates = [<%= data.longitude %>, <%= data.latitude %>];
</script>

<body>
  <div class="container-fluid iss-show py-3 py-md-5">
    <!-- Header -->
    <div class="row justify-content-center mb-3 mb-md-4">
      <div class="col-12 col-md-10 col-lg-8">
        <div class="card p-3 p-md-4 border-0 shadow-sm text-center">
          <div class="card-body">
            <h1 class="h2 fw-bold mb-2 mb-md-3"> ISS Live Tracker</h1>
            <p class="lead fw-semibold">
              Track the International Space Station in real time as it orbits Earth.
            </p>
            <p class="text-muted">
              Below is a live map showing the ISS's current location based on public satellite tracking data.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Map & Coordinates -->
    <div class="row justify-content-center gx-4">
      <!-- Map Column -->
      <div class="col-12 col-lg-8 mb-3 mb-lg-4">
        <div id="map" style="height: 400px; width: 100%; border-radius: 15px; overflow: hidden; box-shadow: 0 0 20px rgba(0,0,0,0.1);"></div>
      </div>

      <!-- Coordinates Column -->
      <div class="col-12 col-lg-4 d-flex align-items-center mb-3 mb-lg-0">
        <div class="card w-100 text-center p-3 p-md-4 shadow-sm border-0" style="border-radius: 15px;">
          <h4 class="fw-bold mb-2 mb-md-3 h5">📍 Current ISS Coordinates</h4>
          <p class="mb-2 fs-6">
            <strong>Latitude:</strong> <span id="lat"><%= data.latitude %></span>
          </p>
          <p class="mb-0 mb-md-2 fs-6">
            <strong>Longitude:</strong> <span id="lon"><%= data.longitude %></span>
          </p>
        </div>
      </div>
    </div>
  </div>

  <!-- Mapbox Script -->
  <script src="/js/map.js"></script>
</body>
